<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Список товаров</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <style> body { background-color: #f8f9fa; } </style>
</head>
<body class="container py-4">
    <h1 class="text-center mb-4">Список товаров</h1>

    <div class="d-flex justify-content-between mb-3">
        <span>Вы вошли как <strong><%= username %></strong></span>
        <a href="/logout" class="btn btn-outline-danger">Выйти</a>
    </div>
    

    <div class="d-flex justify-content-center gap-2 mb-3">
        <a href="/" class="btn btn-primary">Все</a>
        <a href="/?category=Электроника" class="btn btn-info">Электроника</a>
        <a href="/?category=Мебель" class="btn btn-warning">Мебель</a>        
        <a href="/?sort=asc" class="btn btn-success">Цена(по возрастанию)</a>
        <a href="/?sort=desc" class="btn btn-danger">Цена(по убыванию)</a>
    </div>

    <table class="table table-bordered table-striped">
        <thead class="table-dark">
            <tr>
                <th>Название</th>
                <th>Цена $</th>
                <th>Категория</th>
            </tr>
        </thead>
        <tbody>
             <% products.forEach(product => { %>
                <tr>
                    <td><%= product.name %></td>
                    <td>$<%= product.price %></td>
                    <td><%= product.category %></td>
                </tr>
            <% }); %>
        </tbody>
    </table>

    <h2 class="mt-4">Добавить товар</h2>
    <form action="/products" method="POST" class="row g-3">
        <div class="col-md-4">
            <input type="text" name="name" class="form-control" placeholder="Название" required>
        </div>
        <div class="col-md-4">
            <input type="number" name="price" class="form-control" placeholder="Цена" required>
        </div>
        <div class="col-md-4">
            <select name="category" class="form-select" required>
                <option value="" disabled selected>Выберите категорию</option>
                <option value="Электроника">Электроника</option>
                <option value="Мебель">Мебель</option>
            </select>            
        </div>
        <div class="col-12">
            <button type="submit" class="btn btn-success">Добавить</button>
        </div>
    </form>
</body>
</html>
